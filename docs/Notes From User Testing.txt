Notes from User Testing:

Main issue is environment. A lot of "Failed to fetch" issues. After investigation, this is not a code issue but a failure to run the FastAPI backend server (probably due to unclear instructions). Failed to fetch error disappeard when FastAPI was activated properly, and came back again when it was stopped confirming the issue. Main focus after bug bash is to rewrite instructions to be more clear.